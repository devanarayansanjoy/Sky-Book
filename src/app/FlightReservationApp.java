package app;

import model.*;
import data.*;
import ui.*;
import ui.components.*;
import util.*;

public class FlightReservationApp {
    public static void main(String[] args) {
        // Redirect execution to the new modular entry point
        LoginFrame.main(args);
    }
}
